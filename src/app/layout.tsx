import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Providers from "./providers";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Team Inventory | Equipment Management System",
  description:
    "Track and manage your team's equipment, tools, and resources. Monitor availability, holdings, and inventory across your organization.",
  keywords: ["inventory", "equipment", "team management", "asset tracking"],
  authors: [{ name: "Maxim Grivennyy" }],
  openGraph: {
    title: "Team Inventory",
    description: "Manage your team's equipment and resources",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
